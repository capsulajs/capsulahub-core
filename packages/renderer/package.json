{
  "name": "@capsulajs/capsulahub-core-renderer",
  "version": "0.0.1",
  "description": "A renderer package for CapsulaHub core services",
  "main": "lib/index.js",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "yarn build:cjs && yarn build:es && yarn build:dist",
    "build:cjs": "rimraf lib && tsc --module commonjs --target es2015 --outDir lib",
    "build:es": "rimraf es && tsc --module esnext --target es2015 --outDir es",
    "build:dist": "rimraf es && parcel src/index.ts esnext --target es2015",
    "clean": "rimraf lib es",
    "doc": "typedoc ./src/api --out ./doc --mode file --hideGenerator",
    "test": "NODE_ENV=test jest --maxWorkers=4 --env=jsdom --passWithNoTests",
    "lint": "yarn lint:md && yarn lint:ts",
    "lint:md": "remark . --rc-path ../../.remarkrc --output",
    "lint:ts": "tslint '{src,tests}/**/*.{ts,tsx}' --fix",
    "prettier": "prettier --write '{src,tests}/**/*.{ts,tsx}'",
    "start-example": "NODE_ENV=test parcel example/index.html",
    "cypress:debug": "cypress open",
    "cypress:run": "cypress run"
  },
  "jest": {
    "transform": {
      ".(ts|tsx)": "ts-jest"
    },
    "setupFiles": [
      "./tests/setupTests.ts"
    ],
    "testRegex": "/tests/implementations/.*.spec.ts$",
    "moduleFileExtensions": [
      "ts",
      "tsx",
      "js"
    ],
    "moduleDirectories": [
      "node_modules",
      "app/src"
    ]
  },
  "dependencies": {
    "@babel/polyfill": "^7.4.4",
    "@capsulajs/capsulahub-core-workspace": ">=0.0.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "devDependencies": {
    "@types/jsdom": "^12.2.3",
    "@types/node": "^12.0.2",
    "cypress": "^3.3.0",
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-wrapper": "^0.2.2"
  }
}
